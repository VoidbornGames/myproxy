http_port 3128

# Allow all CONNECT requests (HTTPS tunneling)
acl CONNECT method CONNECT
http_access allow CONNECT

# Allow all other requests
acl all src 0.0.0.0/0
http_access allow all

# Disable caching (optional)
cache deny all
